{{- if .Values.lateChunking.enabled -}}
# Copyright (C) 2024-2026 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.lateChunking.nameOverride | default (include "helm-edp.fullname" .) }}
  labels:
    {{- include "helm-edp.lateChunking.selectorLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8003
      targetPort: 8003
      protocol: TCP
      name: http
  selector:
    {{- include "helm-edp.lateChunking.selectorLabels" . | nindent 4 }}
{{- end }}
