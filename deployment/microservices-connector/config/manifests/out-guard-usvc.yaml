---
# Source: out-guard-usvc/templates/configmap.yaml
# Copyright (C) 2024-2025 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

apiVersion: v1
kind: ConfigMap
metadata:
  name: out-guard-usvc-config
  labels:
    {{- include "manifest.labels" (list .filename .) | nindent 4 }}
data:
  {{- include "manifest.addEnvsAndEnvFile" (list .filename .) | nindent 2 }}
  http_proxy: {{ .Values.proxy.httpProxy | quote }}
  https_proxy: {{ .Values.proxy.httpsProxy | quote }}
  no_proxy: {{ .Values.proxy.noProxy | quote }}
  HF_HOME: /tmp/in-guard/huggingface
  KMP_AFFINITY: granularity=fine,compact,1,0
  MKL_NUM_THREADS: "8"
  OMP_NUM_THREADS: "8"
---
# Source: out-guard-usvc/templates/service.yaml
# Copyright (C) 2024-2025 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

apiVersion: v1
kind: Service
metadata:
  name: out-guard-usvc
  labels:
    {{- include "manifest.labels" (list .filename .) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8060
      targetPort: 8060
      protocol: TCP
      name: out-guard-usvc
  selector:
    {{- include "manifest.selectorLabels" (list .filename .) | nindent 4 }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/name: out-guard-usvc
    app.kubernetes.io/instance: out-guard-usvc
  name: out-guard-usvc
---
# Source: out-guard-usvc/templates/deployment.yaml
# Copyright (C) 2024-2025 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

apiVersion: apps/v1
kind: Deployment
metadata:
  name: out-guard-usvc
  labels:
    {{- include "manifest.labels" (list .filename .) | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
    {{- include "manifest.selectorLabels" (list .filename .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "manifest.selectorLabels" (list .filename .) | nindent 8 }}
      {{- include "manifest.tdx.annotations" (list .filename .) | nindent 6 }}
    spec:
      {{- include "manifest.tdx.runtimeClassName" (list .filename .) | nindent 6 }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- include "gmc.imagePullSecrets" . }}
      serviceAccountName: out-guard-usvc
      containers:
        - name: out-guard-usvc
          envFrom:
            - configMapRef:
                name: out-guard-usvc-config
            - configMapRef:
                name: extra-env-config
                optional: true
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ toYaml (index .Values "images" .filename "repository") }}/{{ toYaml (index .Values "images" .filename "image") }}:{{ toYaml (index .Values "images" .filename "tag") }}
          imagePullPolicy: {{ toYaml (index .Values "images" .filename "pullPolicy" | default "Always") }}
          ports:
            - name: out-guard-usvc
              containerPort: 8060
              protocol: TCP
          volumeMounts:
            - mountPath: /tmp
              name: tmp
            - mountPath: /home/user/nltk_data
              name: nltk-data
          livenessProbe:
            failureThreshold: 24
            httpGet:
              path: v1/health_check
              port: out-guard-usvc
            initialDelaySeconds: 90
            periodSeconds: 60
          readinessProbe:
            httpGet:
              path: v1/health_check
              port: out-guard-usvc
            initialDelaySeconds: 90
            periodSeconds: 60
          startupProbe:
            failureThreshold: 120
            httpGet:
              path: v1/health_check
              port: out-guard-usvc
            initialDelaySeconds: 90
            periodSeconds: 60
          resources:
            requests:
              cpu: {{ include "manifest.tdx.values" (dict "default" "4" "name" .filename "Values" .Values "type" "resources.requests.cpu") | trim }}
              memory: {{ include "manifest.tdx.values" (dict "default" "8Gi" "name" .filename "Values" .Values "type" "resources.requests.memory") | trim }}
            limits:
              cpu: {{ include "manifest.tdx.values" (dict "default" "8" "name" .filename "Values" .Values "type" "resources.limits.cpu") | trim }}
              memory: {{ include "manifest.tdx.values" (dict "default" "16Gi" "name" .filename "Values" .Values "type" "resources.limits.memory") | trim }}
      volumes:
        - name: tmp
          emptyDir: {}
        - name: nltk-data
          emptyDir: {}
