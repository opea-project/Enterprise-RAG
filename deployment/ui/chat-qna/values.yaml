# Default values for helm-ui.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

proxy:
  httpProxy: ""
  httpsProxy: ""
  noProxy: ""

image:
  - ui:
      repository: "205130860845.dkr.ecr.us-west-2.amazonaws.com/opea/chatqna-conversation-ui"
      pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
      tag: "latest"
  - fingerprint:
      repository: "localhost:5000/system-fingerprint"
      tag: "latest"

imagePullSecrets:
  - name: regcred
nameOverride: "ui-app"
fpNameOverride: "fingerprint-usvc"
mongoNameOverride: "mongodb"
fullnameOverride: "ui-chart"

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations:
    kubernetes.io/ingress.class: "nginx"
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: "chat-qna-ui-app"

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 4173

aliasIP: localhost

mongodb:
  name: SYSTEM_FINGERPRINT
  svc: mongodb
  port: 27017

ingress:
  enabled: true
  className: nginx
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  tls:
    - hosts:
      - erag.com
      secretName: tls-secret
  hosts:
    - host: erag.com
      paths:
        - pathType: Prefix
          path: /
          backend:
            service:
              name: ui-chart
              port:
                number: 4173

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}
