FROM chat-history-base:latest

COPY compliance /compliance

WORKDIR /compliance/ospdt
COPY Dockerfile .
COPY uv.lock .

# CMD from base Dockerfile
WORKDIR /home/user/comps/chat_history
ENTRYPOINT ["python", "opea_chat_history_microservice.py"]
