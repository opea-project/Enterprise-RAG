apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: embedding-svc
  labels:
    release: rag-telemetry
spec:
  namespaceSelector:
    matchNames:
    - chatqa
  selector:
    matchLabels:
      app.kubernetes.io/instance: embedding-usvc
  endpoints:
  - port: "embedding-usvc"
