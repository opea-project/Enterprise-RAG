apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: retriever-svc
  labels:
    release: rag-telemetry
spec:
  namespaceSelector:
    matchNames:
    - chatqa
  selector:
    matchLabels:
      app.kubernetes.io/instance: retriever-usvc
  endpoints:
  - port: "retriever-usvc"
